import 'package:flutter/material.dart';

class LockIcon {
  Icon lock = const Icon(
    Icons.remove_red_eye_outlined,
    size: 20,
  );
  Icon open = const Icon(
    Icons.remove_red_eye,
    size: 20,
  );
}
